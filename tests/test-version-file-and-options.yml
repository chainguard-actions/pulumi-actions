name: Test pulumi-version-file and operation options
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create pulumi version file
        run: echo "3.140.0" > /tmp/.pulumi.version
      - id: action
        uses: .
        with:
          command: preview
          stack-name: dev
          pulumi-version-file: /tmp/.pulumi.version
          work-dir: ./
          cloud-url: file://~
          message: 'test update message'
          parallel: '4'
          config-map: '{"key": {"value": "val", "secret": false}}'
          color: never
          suppress-outputs: 'true'
          suppress-progress: 'true'
        continue-on-error: true
      - name: Assert action ran
        run: |
          echo "Action completed (may fail without a real Pulumi project, but inputs were accepted)"
          echo "PASS"
